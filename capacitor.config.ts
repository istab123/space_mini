import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.spacemini',
  appName: 'space_mini',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
